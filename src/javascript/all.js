window.addEventListener('load', function () {
    const currentTitle = document.title;
    document.title = `${currentTitle} | Macbeth Project / Macbeth into madness...`;
    console.log('Page has successfully loaded!');
});
